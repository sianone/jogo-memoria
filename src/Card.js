import React from "react";

const checkFlip = {};

const Cards = (props) => {
  const { card, flipped } = props;
  console.log(props);

  return (
    <div>
      <div value></div>
    </div>
  );
};

export default Cards;
